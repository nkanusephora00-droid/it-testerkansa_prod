/**
 * Alias de compatibilité : /tests et /test-sessions affichent TestSessions.tsx.
 * Le code de la page est dans ./TestSessions.tsx (pas dans ce fichier).
 */
export { default } from "./TestSessions";
